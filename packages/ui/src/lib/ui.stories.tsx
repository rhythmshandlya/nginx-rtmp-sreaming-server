import type { Meta } from '@storybook/react';
import { Ui } from './ui';

const meta: Meta<typeof Ui> = {
  component: Ui,
  title: 'Ui',
};
export default meta;

export const Primary = {
  args: {},
};
